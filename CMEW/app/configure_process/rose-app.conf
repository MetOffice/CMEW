[command]
default=cmew-process-env configure_process.py
       =cmew-process-env esmvaltool recipes get recipe_radiation_budget.yml && \
       =mkdir -p ${CYLC_WORKFLOW_SHARE_DIR}/etc/ && \
       =mv recipe_radiation_budget.yml ${CYLC_WORKFLOW_SHARE_DIR}/etc/recipe_radiation_budget.yml
